<Window x:Class="ServisVozila.Views.VozilaInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServisVozila.Views"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Info vozila"
        WindowStartupLocation="CenterScreen"
        Height="327" Width="414"
        Icon="\Images\info-50.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="txtNaziv" Text="{Binding Naziv}" FontFamily="Calibri" FontWeight="SemiBold" Grid.Row="1" Grid.Column="1" />
        <TextBlock x:Name="txtRegOznaka" Text="{Binding RegOznaka}" FontFamily="Consolas" Foreground="DarkGray" FontSize="12" Grid.Row="2" Grid.Column="1" Margin="0,5,0,0"/>
        <TextBlock x:Name="txtKW" Text="{Binding kW, StringFormat={}{0} kW}" FontFamily="Consolas"  FontSize="12" Grid.Row="3" Grid.Column="1" Margin="0,5,0,0"/>
        <TextBlock x:Name="txtGodina" Text="{Binding GodProizvodnje}" FontFamily="Consolas"  FontSize="12" Grid.Row="4" Grid.Column="1" Margin="0,5,0,0"/>
        <TextBlock x:Name="txtVrstaGoriva" Text="{Binding VrstaGoriva}" FontFamily="Consolas"  FontSize="12" Grid.Row="5" Grid.Column="1" Margin="0,5,0,0"/>
        <TextBlock x:Name="txtKilometraza" Text="{Binding Kilometraza, StringFormat={}{0} km}" FontFamily="Consolas" FontSize="12" Grid.Row="6" Grid.Column="1" Margin="0,5,0,0"/>
        <TextBlock x:Name="txtRegistracijaDo" Text="{Binding RegistracijaDo, StringFormat={}Registracija do: {0:dd.MM.yyyy}}"  FontFamily="Consolas" Foreground="DodgerBlue" FontSize="12" Grid.Row="7" Grid.Column="1" Margin="0,5,0,0"/>


        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/> 
                <ColumnDefinition Width="*"/> 
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            
            
            
        </Grid>



        </Grid>
</Window>
